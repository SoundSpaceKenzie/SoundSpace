import { LoginFormContainer } from './style';

const LoginForm = () => {
  const test = 0;

  return (
    <LoginFormContainer>
      <h2>test</h2>
    </LoginFormContainer>
  );
};

export default LoginForm;
